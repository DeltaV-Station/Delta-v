# Example popup

- type: feedbackPopup
  id: ExampleFeedbackPopup
  popupName: "FeedbackPopupCampaign"
  title: "[color=red]FEEDBACK[/color] on new popup system."
  description:
  - "This is a [bold]new system[/bold] to get feedback on features. It will give popups!"
  - "It also supports multiple sections if it looks better."
  feedbackField: true # Defaults to true but can be set to false

# Please make a new file in this folder for each popup. This will greatly reduce the number of merge conflicts that will occur!
