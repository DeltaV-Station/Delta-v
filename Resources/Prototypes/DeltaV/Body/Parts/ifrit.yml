- type: entity
  parent: WelderIfrit
  id: WelderIfritRight # The body part that spawns in the hand, does NOT work as a hand but it gets the job done.
  name: ifrit hand
  components:
  - type: BodyPart
    partType: Hand
    symmetry: Right
