<!--
SPDX-FileCopyrightText: 2021 Visne <39844191+Visne@users.noreply.github.com>
SPDX-FileCopyrightText: 2022 EmoGarbage404 <98561806+EmoGarbage404@users.noreply.github.com>
SPDX-FileCopyrightText: 2022 Kara <lunarautomaton6@gmail.com>
SPDX-FileCopyrightText: 2022 Paul Ritter <ritter.paul1@googlemail.com>
SPDX-FileCopyrightText: 2022 metalgearsloth <31366439+metalgearsloth@users.noreply.github.com>
SPDX-FileCopyrightText: 2022 wrexbe <81056464+wrexbe@users.noreply.github.com>
SPDX-FileCopyrightText: 2025 Aiden <28298836+Aidenkrz@users.noreply.github.com>

SPDX-License-Identifier: MIT
-->
<!-- basically all of this has been changed by goobstation to a new UI, See Goobstation PR #4780-->
<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    MinSize="540 390"
    SetSize="1045 600">

    <!-- Main Container -->
    <BoxContainer Orientation="Vertical"
        VerticalExpand="True">

        <TabContainer Name="TabContainer" VerticalExpand="True">
          <!-- Sub-Main Container -->
          <BoxContainer Orientation="Horizontal"
              VerticalExpand="True"
              Margin="8 4 8 6">

              <!-- Left Part -->
              <BoxContainer Orientation="Vertical"
                  SeparationOverride="4"
                  Margin="0 0 8 0"
                  HorizontalExpand="True">

                  <!-- Info -->
                  <BoxContainer Orientation="Vertical">
                      <GridContainer Columns="3">

                          <!-- Account -->
                          <Label Text="{Loc 'cargo-console-menu-account-name-label'}"
                              StyleClasses="LabelKeyText" />

                          <PanelContainer StyleClasses="LowDivider" Margin="0 -4"/>

                          <RichTextLabel Name="AccountNameLabel"
                              Text="{Loc 'cargo-console-menu-account-name-none-text'}"
                              Margin="4 0"/>

                          <!-- Balance -->
                          <Label Text="{Loc 'cargo-console-menu-points-label'}"
                              StyleClasses="LabelKeyText"/>

                          <PanelContainer StyleClasses="LowDivider" Margin="0 -2"/>

                          <RichTextLabel Name="PointsLabel"
                              Text="$0"
                              Margin="4 0" />

                          <!-- Orders Count/Capacity -->
                          <Label Text="{Loc 'cargo-console-menu-order-capacity-label'}"
                              StyleClasses="LabelKeyText" />

                          <PanelContainer StyleClasses="LowDivider" Margin="0 -2 0 -4"/>

                          <Label Name="ShuttleCapacityLabel"
                              Text="0/20"
                              Margin="4 0"/>
                      </GridContainer>

                      <PanelContainer StyleClasses="LowDivider" Margin="0 4.5 -8 0"/>
                  </BoxContainer>

                  <!-- Search -->
                  <BoxContainer Orientation="Horizontal"
                      Margin="0 2 0 0">

                      <LineEdit Name="SearchBar"
                          PlaceHolder="{Loc 'cargo-console-menu-search-bar-placeholder'}"
                          HorizontalExpand="True" />

                      <OptionButton Name="Categories"
                          Prefix="{Loc 'cargo-console-menu-categories-label'}"
                          StyleClasses="OpenLeft"/>
                  </BoxContainer>

                  <PanelContainer VerticalExpand="True"
                                  Margin="0 0 0 0">

                  <!-- Product list -->
                  <ScrollContainer
                      HorizontalExpand="False"
                      VerticalExpand="True"
                      HScrollEnabled="False">

                      <BoxContainer Name="Products"
                          Orientation="Vertical"
                          HorizontalExpand="True"
                          VerticalExpand="True">

                          <!-- Products get added here by code -->
                      </BoxContainer>
                  </ScrollContainer>
                  </PanelContainer>
              </BoxContainer>

              <PanelContainer StyleClasses="LowDivider" Margin="-2 37 0 0"/>

              <!-- Right Part -->
              <BoxContainer Orientation="Vertical"
                  SizeFlagsStretchRatio="1.1"
                  HorizontalExpand="True"
                  Name="RightPart">

                  <!-- Requests Part -->
                  <BoxContainer Orientation="Vertical"
                      VerticalExpand="True"
                      SizeFlagsStretchRatio="2">

                      <!-- Title -->
                      <controls:StripeBack>
                          <Label Text="{Loc 'cargo-console-menu-requests-label'}"
                              HorizontalAlignment="Center"
                              Margin="4"/>
                      </controls:StripeBack>

                      <PanelContainer VerticalExpand="True"
                          Margin="0 -4 0 0">

                          <!-- Background -->
                          <PanelContainer.PanelOverride>
                              <gfx:StyleBoxFlat BackgroundColor="#040404" />
                          </PanelContainer.PanelOverride>
                          <BoxContainer Orientation="Vertical">
                              <ScrollContainer VerticalExpand="True">
                                  <BoxContainer Name="Requests"
                                      Orientation="Vertical"
                                      StyleClasses="transparentItemList"
                                      VerticalExpand="True"
                                      SeparationOverride="8"
                                      Margin="8">

                                      <!-- Requests are added here by code -->
                                  </BoxContainer>
                              </ScrollContainer>
                          </BoxContainer>
                      </PanelContainer>
                  </BoxContainer>
              </BoxContainer>
          </BoxContainer>
          <!-- Funds tab -->
          <BoxContainer Orientation="Vertical" Margin="8 4 8 6">
                    <!-- Info -->
            <BoxContainer Orientation="Vertical">
                      <GridContainer Columns="3">

                          <!-- Account -->
                          <Label Text="{Loc 'cargo-console-menu-account-name-label'}"
                              StyleClasses="LabelKeyText" />

                          <PanelContainer StyleClasses="LowDivider" Margin="0 -2"/>

                          <RichTextLabel Name="AccountNameLabelFundsTransfer"
                              Text="{Loc 'cargo-console-menu-account-name-none-text'}"
                              Margin="4 0"/>

                          <!-- Balance -->
                          <Label Text="{Loc 'cargo-console-menu-points-label'}"
                              StyleClasses="LabelKeyText"/>

                          <PanelContainer StyleClasses="LowDivider" Margin="0 -2"/>

                          <RichTextLabel Name="PointsLabelFundsTransfer"
                              Text="$0"
                              Margin="4 0" />

                          <Label Text="{Loc 'cargo-console-menu-account-action-transfer-limit'}"
                              StyleClasses="LabelKeyText" />

                          <PanelContainer StyleClasses="LowDivider" Margin="0 -2 0 -1"/>

                            <BoxContainer Orientation="Horizontal"
                                        HorizontalExpand="True"
                                        SeparationOverride="4">
                                <Label Name="TransferLimitLabel"
                                    Text=""
                                    Margin="4 0"/>
                                <RichTextLabel Name="UnlimitedNotifier"
                                    Text="{Loc 'cargo-console-menu-account-action-transfer-limit-unlimited-notifier'}"
                                    Visible="False" />
                            </BoxContainer>

                      </GridContainer>

                      <PanelContainer StyleClasses="LowDivider" Margin="0 4.5 -8 0"/>
            </BoxContainer>
              <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
              </BoxContainer>
              <BoxContainer Orientation="Horizontal">
                  <RichTextLabel Text="{Loc 'cargo-console-menu-account-action-select'}" Margin="0 0 10 0"/>
                  <OptionButton Name="ActionOptions"/>
              </BoxContainer>
              <Control MinHeight="5"/>
              <BoxContainer Orientation="Horizontal">
                  <RichTextLabel Name="AmountText" Text="{ Loc 'cargo-console-menu-account-action-amount'}"/>
                  <SpinBox Name="TransferSpinBox" MinWidth="100" Value="10"/>
              </BoxContainer>
                <BoxContainer Orientation="Horizontal" HorizontalExpand="True" VerticalExpand="True" VerticalAlignment="Bottom" Margin="8">
                    <!-- Left button -->
                    <Button Name="AccountLimitToggleButton"
                            Text="{Loc 'cargo-console-menu-toggle-account-lock-button'}"
                            MinHeight="45"
                            MinWidth="120"/>

                    <!-- Spacer takes all remaining space -->
                    <Control HorizontalExpand="True"/>

                    <!-- Right button -->
                    <Button Name="AccountActionButton"
                            Text="{Loc 'cargo-console-menu-account-action-button'}"
                            MinHeight="45"
                            MinWidth="120"/>
                </BoxContainer>
          </BoxContainer>
        </TabContainer>

        <!-- Footer -->
        <!-- TODO: Create customControls element -->
        <BoxContainer Orientation="Vertical"
            VerticalAlignment="Bottom">


            <BoxContainer Orientation="Horizontal"
                Margin="12 0 6 2"
                VerticalAlignment="Bottom">

                <!-- Footer title -->
                <Label Text="{Loc 'cargo-console-menu-flavor-left'}"
                    StyleClasses="WindowFooterText" />

                <!-- Version -->
                <Label Text="{Loc 'cargo-console-menu-flavor-right'}"
                    StyleClasses="WindowFooterText"
                    HorizontalAlignment="Right"
                    HorizontalExpand="True"
                    Margin="0 0 4 0" />

                <TextureRect StyleClasses="NTLogoDark"
                    Stretch="KeepAspectCentered"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
